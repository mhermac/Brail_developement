import 'package:flutter/material.dart';

import 'negotiations_page.dart';

class DriverNegotiationScreen extends StatelessWidget {
  const DriverNegotiationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return NegotiationPage();
  }
}
