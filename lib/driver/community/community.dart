import 'package:flutter/material.dart';

class DriverCommunityScreen extends StatelessWidget {
  const DriverCommunityScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Driver community'),
    );
  }
}
